apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  namespace: "gitdep"
  labels:
    app: dep1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: dep1
  template:
    metadata:
      labels:
        app: dep1
    spec:
      containers:
      - name: pod1
        image: nginx:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
 name: lbservice
 namespace: "gitdep"
spec:
 selector:
  app: dep1
 ports:
  - port: 8082
 type: LoadBalancer

